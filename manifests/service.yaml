apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  namespace: ai-agent
  labels:
    app: ai-agent
spec:
  type: ClusterIP
  selector:
    app: ai-agent
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
